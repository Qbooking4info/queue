{
  "name": "@queue/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Code shared by the client and provider apps: Supabase access, the API layer, auth/theme contexts, maps, and the Agora call configuration. Anything imported by BOTH apps belongs here -- keeping one copy is the whole point, since a hand-synced second copy is what silently shipped a two-versions-behind call screen before.",
  "main": "index.ts",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "29.5.14",
    "jest": "~29.7.0",
    "jest-expo": "~56.0.5"
  },
  "jest": {
    "preset": "jest-expo",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  }
}
